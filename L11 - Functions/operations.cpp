// File Management kind of thing
// This way, we can store definitions in other files
// and just reference them when we need them for the main file

int incr_mult(int a, int b){
    return ((++a) * (++b));
}